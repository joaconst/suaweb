<section class="relative flex flex-col items-center justify-center mt-8 md:mt-32 px-4">
    <img
        src="/home/4.webp"
        alt="Decoración abstracta"
        class="absolute top-2 left-0 w-16 md:w-24 lg:w-60 opacity-20 -z-0 pointer-events-none"
        aria-hidden="true"
    />

    <img
        src="/home/5.webp"
        alt="Decoración abstracta"
        class="absolute bottom-2 right-0 w-16 md:w-24 lg:w-60 opacity-20 -z-0 pointer-events-none"
        aria-hidden="true"
    />

    <h2 class="text-3xl md:text-4xl lg:text-6xl font-extrabold text-center px-4 mb-8 md:mb-16">
        ¡Elegí la que más se adapte a vos!
    </h2>

    <div class="relative z-10 w-full max-w-7xl mx-auto">
        <!-- Botones de navegación -->
        <button class="nav-prev hidden md:block absolute left-0 xl:-left-14 top-1/2 -translate-y-1/2 z-10 bg-black/80 hover:bg-black text-white p-3 rounded-full shadow-lg transition-all duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
            </svg>
        </button>
        
        <button class="nav-next hidden md:block absolute right-0 xl:-right-14 top-1/2 -translate-y-1/2 z-10 bg-black/80 hover:bg-black text-white p-3 rounded-full shadow-lg transition-all duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
            </svg>
        </button>

        <!-- Slider -->
        <div class="swiper w-full">
            <div class="swiper-wrapper">
                <!-- Card 1 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header bg-primary">
                            <h3>1. LANDING PAGE</h3>
                        </div>
                        <div class="card-body bg-background_secondary">
                            <span>Página web diseñada para captar clientes o promocionar un producto o servicio.</span>
                            <button class="bg-white hover:bg-primary hover:text-white transition-colors">
                                VER MÁS
                            </button>
                        </div>
                    </article>
                </div>

                <!-- Card 2 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header bg-secondary">
                            <h3>2. TIENDA ONLINE (E-COMMERCE)</h3>
                        </div>
                        <div class="card-body bg-primary">
                            <span>Un sitio web diseñado para vender productos o servicios de manera directa, con carrito de compras y pasarelas de pago integradas.</span>
                            <button class="bg-white hover:bg-secondary hover:text-white transition-colors">
                                VER MÁS
                            </button>
                        </div>
                    </article>
                </div>

                <!-- Card 3 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header bg-primary">
                            <h3>3. SITIO WEB CORPORATIVO</h3>
                        </div>
                        <div class="card-body bg-background_secondary">
                            <span>Una página web completa que refleja la identidad y valores de una empresa, diseñada para generar confianza y comunicar profesionalismo.</span>
                            <button class="bg-white hover:bg-primary hover:text-white transition-colors">
                                VER MÁS
                            </button>
                        </div>
                    </article>
                </div>

                <!-- Card 4 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header bg-secondary">
                            <h3>4. BLOG PERSONAL / PORTFOLIO</h3>
                        </div>
                        <div class="card-body bg-primary">
                            <span>Un espacio en línea diseñado para mostrar proyectos, publicaciones o servicios de manera profesional y ordenada.</span>
                            <button class="bg-white hover:bg-secondary hover:text-white transition-colors">
                                VER MÁS
                            </button>
                        </div>
                    </article>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .card-content {
        position: relative;
        width: 100%;
        max-width: 340px;
        height: 400px;
        margin: 0 auto;
        border-radius: 1.5rem;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    .card-header {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4rem;
        border-top-left-radius: 1.5rem;
        border-top-right-radius: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 10;
    }

    .card-header h3 {
        color: white;
        font-weight: 700;
        font-size: 0.875rem;
        padding: 0 1rem;
        text-align: center;
        line-height: 1.2;
    }

    .card-body {
        height: 100%;
        display: flex;
        flex-direction: column;
        padding: 4rem 1rem 1.5rem;
        border-radius: 1.5rem;
    }

    .card-body span {
        color: white;
        font-size: 1rem;
        font-weight: 600;
        text-align: center;
        padding: 1rem;
        flex-grow: 1;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .card-body button {
        margin: 0 0.5rem;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        border-radius: 2rem;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    /* Swiper Styles */
    .swiper {
        width: 100%;
        padding: 2rem 0;
        overflow: visible;
    }

    .swiper-slide {
        transition:
            transform 0.6s cubic-bezier(0.4, 0, 0.2, 1),
            opacity 0.4s ease;
        opacity: 0.5;
        transform: scale(0.85);
        filter: blur(1px);
    }

    .swiper-slide-visible {
        opacity: 1;
        transform: scale(1);
        filter: none;
        z-index: 1;
    }

    /* Mobile Adaptations */
    @media (max-width: 767px) {
        .swiper {
            padding: 1.5rem 0;
        }

        .card-content {
            height: 380px;
            max-width: 300px;
        }

        .card-header {
            height: 3.5rem;
        }

        .card-header h3 {
            font-size: 0.75rem;
        }

        .card-body span {
            font-size: 0.875rem;
            padding: 0.5rem;
        }

        .card-body button {
            padding: 0.5rem 1rem;
            font-size: 0.875rem;
        }

        .swiper-slide {
            transform: scale(0.8);
            opacity: 0.4;
        }

        .swiper-slide-visible {
            transform: scale(1);
            opacity: 1;
        }
    }

    @media (min-width: 768px) {
        .card-content {
            height: 480px;
            max-width: 400px;
        }

        .card-header h3 {
            font-size: 1.125rem;
        }

        .card-body span {
            font-size: 1.25rem;
            padding: 1.5rem;
        }
    }
</style>

<script>
    import Swiper from 'swiper';
    import { Navigation } from 'swiper/modules';

    const initSwiper = () => {
        return new Swiper('.swiper', {
            modules: [Navigation],
            loop: true,
            speed: 600,
            centeredSlides: true,
            slideActiveClass: 'swiper-slide-visible',
            breakpoints: {
                320: {
                    slidesPerView: 1,
                    spaceBetween: 20,
                },
                480: {
                    slidesPerView: 1.2,
                    spaceBetween: 24,
                },
                768: {
                    slidesPerView: 1.5,
                    spaceBetween: 32,
                },
                1024: {
                    slidesPerView: 2.2,
                    spaceBetween: 40,
                },
                1280: {
                    slidesPerView: 3,
                    spaceBetween: 50,
                }
            },
            navigation: {
                nextEl: '.nav-next',
                prevEl: '.nav-prev',
            },
        });
    };

    document.addEventListener('DOMContentLoaded', initSwiper);
</script>