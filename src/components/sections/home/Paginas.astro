---

---

<section
    class="relative w-full max-w-sm md:max-w-4xl lg:max-w-6xl xl:max-w-7xl mx-auto flex flex-col items-center justify-center text-center mt-16"
>
    <img
        src="/home/4.webp"
        alt=""
        class="absolute top-35 sm:-top-10 sm:-left-25 left-0 w-24 md:w-60 opacity-20 -z-0"
        aria-hidden="true"
    />

    <img
        src="/home/5.webp"
        alt=""
        class="absolute bottom-0 md:-boottom-50 lg:bottom-20 right-0 md:-right-40 lg:-right-25 w-24 md:w-60 opacity-20 -z-0"
        aria-hidden="true"
    />

    <h2 class="text-5xl lg:text-6xl font-extrabold">
        ¡Elegí la que más se adapte a vos!
    </h2>

    <div class="relative z-10 w-full mx-auto flex">
        <!-- Botón de navegación anterior -->
        <button
            class="nav-prev absolute left-0 xl:-left-14 top-1/2 -translate-y-1/2 z-10 bg-black p-3 rounded-full shadow-lg hover:bg-gray-50 transition-all"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="w-6 h-6"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M15.75 19.5L8.25 12l7.5-7.5"></path>
            </svg>
        </button>

        <!-- Botón de navegación siguiente -->
        <button
            class="nav-next absolute right-0 xl:-right-14 top-1/2 -translate-y-1/2 z-10 bg-white p-3 rounded-full shadow-lg hover:bg-gray-50 transition-all"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="w-6 h-6"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M8.25 4.5l7.5 7.5-7.5 7.5"></path>
            </svg>
        </button>

        <!-- Contenedor del Slider -->
        <div class="swiper">
            <div class="swiper-wrapper">
                <!-- Card 1 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header">
                            <h3>1. LANDING PAGE</h3>
                        </div>
                        <div class="card-body bg-background_secondary">
                            <span
                                >Página web diseñada para captar clientes o
                                promocionar un producto o servicio.
                            </span>
                            <button
                                class="bg-white hover:bg-primary transition-colors"
                                >VER MÁS</button
                            >
                        </div>
                    </article>
                </div>

                <!-- Card 2 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header">
                            <h3>2. TIENDA OLINE (E-COMMERCE)</h3>
                        </div>
                        <div class="card-body bg-primary">
                            <span
                                >Un sitio web diseñado para vender productos o
                                servicios de manera directa, con carrito de
                                compras y pasarelas de pago integradas.</span
                            >
                            <button
                                class="bg-white hover:bg-background_secondary transition-colors"
                                >VER MÁS</button
                            >
                        </div>
                    </article>
                </div>

                <!-- Card 3 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header">
                            <h3>3. SITIO WEB CORPORATIVO</h3>
                        </div>
                        <div class="card-body bg-background_secondary">
                            <span
                                >Una página web completa que refleja la
                                identidad y valores de una empresa, diseñada
                                para generar confianza y comunicar
                                profesionalismo.</span
                            >
                            <button
                                class="bg-white hover:bg-primary transition-colors"
                                >VER MÁS</button
                            >
                        </div>
                    </article>
                </div>

                <!-- Card 4 -->
                <div class="swiper-slide">
                    <article class="card-content">
                        <div class="card-header">
                            <h3>4. BLOG PERSONAL / PORTFOLIO</h3>
                        </div>
                        <div class="card-body bg-primary">
                            <span
                                >Un espacio en línea diseñado para mostrar
                                proyectos, publicaciones o servicios de manera
                                profesional y ordenada.</span
                            >
                            <button
                                class="bg-white hover:bg-background_secondary transition-colors"
                                >VER MÁS</button
                            >
                        </div>
                    </article>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .card-content {
        position: relative;
        width: 80%;
        height: 28rem;
        border-radius: 1rem;
        transition: all 0.3s ease;
    }

    .card-header {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 3.5rem;
        border-top-left-radius: 2rem;
        border-top-right-radius: 2rem;
        display: flex;
        align-items: center;
        justify-content: center;
        border-bottom: 2px solid black;
    }

    .card-header h3 {
        color: white;
        font-weight: bold;
        font-size: 1rem;
        padding: 0 0.5rem;
        text-align: center;
    }

    .card-body {
        height: 100%;
        display: flex;
        flex-direction: column;
        padding-top: 3.5rem;
        padding-bottom: 1rem;
        border-radius: 2rem;
        border: 2px solid black;
    }

    .card-body span {
        color: white;
        font-size: 1.25rem;
        font-weight: 700;
        text-align: center;
        padding: 30px;
        flex-grow: 1;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .card-body button {
        margin: 0 1rem;
        padding: 0.75rem 0;
        font-weight: 600;
        border-radius: 24px;
        border: none;
        transition: background-color 0.3s ease;
    }

    /* Estilos Swiper */
    .swiper {
        max-width: 100%;
        margin: 0 auto;
        overflow: hidden;
        padding: 5rem 0;
    }

    .swiper-slide {
        transition:
            transform 0.6s ease,
            opacity 0.6s ease,
            scale 0.6s ease,
            visibility 0.6s ease;
        opacity: 0;
        visibility: hidden;
        transform: scale(0.85) translateX(-100%);
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .swiper-slide-visible {
        opacity: 1;
        visibility: visible;
        transform: scale(0.95) translateX(0);
    }

    .swiper-slide-prev {
        transform: scale(0.95) translateX(-100%);
    }

    .swiper-slide-next {
        transform: scale(0.95) translateX(100%);
    }

    /* Botones de navegación */
    .nav-prev,
    .nav-next {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(2px);
        border: 1px solid rgba(0, 0, 0, 0.1);
    }
    .nav-prev:hover,
    .nav-next:hover {
        transform: scale(1.1);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    /* Responsive */
    @media (min-width: 640px) {
        .card-content {
            height: 32rem;
        }

        .card-header h3 {
            font-size: 1rem;
        }

        .card-body span {
            font-size: 1.5rem;
            padding-left: 50px;
        }

        .card-body button {
            font-size: 1rem;
        }
    }

    @media (max-width: 768px) {
        .swiper-slide {
            transform: scale(0.75) translateX(-100%);
        }

        .swiper-slide-visible {
            transform: scale(0.85) translateX(0);
        }

        .swiper-slide-prev {
            transform: scale(0.85);
        }

        .swiper-slide-next {
            transform: scale(0.85);
        }
    }

    @media (min-width: 1024px) {
        .swiper-slide-prev {
            transform: scale(0.95);
        }

        .swiper-slide-next {
            transform: scale(0.95);
        }
    }
</style>

<script>
    import Swiper from "swiper";
    import { Navigation } from "swiper/modules";
    import "swiper/css";

    // Inicializar Swiper
    const swiper = new Swiper(".swiper", {
        modules: [Navigation],
        grabCursor: true,
        centeredSlides: true,
        slidesPerView: "auto",
        initialSlide: 1,
        loop: true,
        speed: 600,
        watchSlidesProgress: true,

        // Configuración responsive
        breakpoints: {
            320: {
                slidesPerView: 1,
                spaceBetween: -50,
            },
            768: {
                slidesPerView: 2.2,
                spaceBetween: -20,
            },
            1024: {
                slidesPerView: 3,
                spaceBetween: -20,
            },
        },

        // Navegación personalizada
        navigation: {
            nextEl: ".nav-next",
            prevEl: ".nav-prev",
        },
    });
</script>
