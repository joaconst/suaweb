---
---

<section 
    id="contacto"
    class="bg-background min-h-screen flex items-center justify-center p-4 sm:p-6 md:p-8 sticky top-0"
>
    <article class="relative w-screen md:w-[90%] md:min-h-[80%] grid grid-cols-1 md:grid-cols-2 items-center bg-fourth rounded-4xl overflow-hidden shadow-xl pt-5 pb-5">
        <!-- Bordes del article -->
        <div class="absolute top-0 left-0 w-full h-10 bg-white z-10 flex items-center gap-x-4">
            <span class="w-6 h-6 bg-yellow-400 rounded-full ml-8 md:ml-16"></span>
            <span class="w-6 h-6 bg-yellow-400 rounded-full"></span>
            <span class="w-6 h-6 bg-yellow-400 rounded-full"></span>
        </div>
        <div class="absolute bottom-0 left-0 w-full h-10 bg-white z-10"></div>

        <!-- Titulo -->
        <div class="flex items-center justify-center pt-6 md:p-8">
            <h2 class="text-white text-2xl md:text-4xl lg:text-5xl xl:text-6xl mt-4 md:mt-0 font-extrabold leading-tight text-center">
                ¿ Y SI NOS CONTÁS TU IDEA ?
            </h2>
        </div>

        <!-- Contenido -->
        <div class="flex items-center justify-center p-8">
            <form class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-2xl p-4 md:p-8 w-full md:space-y-6">
                <div class="space-y-2">
                    <div class="form-element">
                        <span class="text-xl lg:text-2xl font-bold mb-4">Nombre</span>
                        <input
                            name="nombre"
                            type="text"
                            class="w-full rounded-3xl px-4 bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>

                    <div class="form-element">
                        <span class="text-xl lg:text-2xl font-bold mb-4">Correo electrónico</span>
                        <input
                            name="email"
                            type="email"
                            class="w-full rounded-3xl px-4 bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>

                    <div class="form-element">
                        <span class="text-xl lg:text-2xl font-bold mb-4">Teléfono</span>
                        <input
                            name="telefono"
                            type="text"
                            class="w-full rounded-3xl px-4 bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>

                    <div class="form-element">
                        <span class="text-xl lg:text-2xl font-bold mb-4">Mensaje</span>
                        <textarea
                            name="mensaje"
                            class="w-full rounded-3xl px-4 py-1 bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                            rows="4"></textarea>
                    </div>

                    <div class="form-element">
                        <button
                            type="submit"
                            class="w-full text-xl lg:text-2xl bg-fourth text-white py-3 px-6 rounded-3xl font-bold hover:bg-purple-400"
                        >
                            Enviar Mensaje
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </article>
</section>
